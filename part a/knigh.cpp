#include <iostream>#include "chess_p.cpp" // Include the base class header#include <string>class knigh : public chess_p{public:	// Constructor	knigh(std::string start_loc, bool is_it_white);	bool is_move_ok(std::string state_of_board) override;	void move() override;	// Override the is_move_ok method for knight-specific movement logic	bool is_move_ok(std::string state_of_board) override	{		// Implement knight movement logic here		// Knights move in an "L" shape: two squares in one direction and then one square perpendicular		// This is a placeholder implementation; actual logic will depend on the board representation		return true; // Placeholder return value	}	void move(std::string dest) override	{		if (is_move_ok(dest))		{			location = dest;		}	}};